name: host-regression

on:
  push:
    branches:
      - main
      - codex/**
  pull_request:

jobs:
  host-regression:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run fixture regression matrix
        run: make -f host.mk regression
